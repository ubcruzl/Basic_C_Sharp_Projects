using System;
using System.Collections.Generic;

class Program
{/*
    static void Main()
    {
        
        int[] numArray = new int[30];
        for (int i = 0; i < 30; i++)
        {
            numArray[i] = (int)Math.Pow(2, i);
        }
        for (int i = 0; i < 30; i++)
        {
            Console.WriteLine(i+" Array: " + numArray[i]);
        }

        int[] numArrayL = new int[] { 5, 2, 10, 200, 500 };
        Console.WriteLine("numArrayList"+ numArrayL[3]);
        int[] numArrayM = { 5, 2, 10, 200, 500,600,900 };
        Console.WriteLine("numArrayList" + numArrayM[5]);
        Console.ReadLine();
        
    //Una matriz es un método para organizar datos en un dispositivo de memoria.
    //Una lista es una estructura de datos que admite varias operaciones.
    //Una matriz es una colección de partes homogéneas,
    //mientras que una lista consta de elementos heterogéneos.
    //La memoria de matriz es estática y continua.
    //La memoria de listas es dinámica y aleatoria.
    //Los usuarios no necesitan limitar el seguimiento de la próxima memoria con matrices.
    //Con las listas, un usuario tiene que rastrear la siguiente ubicación.

    List<int> intList = new List<int>();
        intList.Add(4);
        intList.Add(10);
        intList.Add(20);
        intList.Add(30);
        intList.Add(40);
        Console.WriteLine("List " + intList[0]);


        List<int> intList1 = new List<int>();
        for (int i = 0; i < 30; i++)
        {
            intList1.Add((int)Math.Pow(2, i));
        }
        intList1.Remove(1); //remove the occurrence
        for (int i = 0; i < 29; i++)
        {
            Console.WriteLine(i + " List: " + intList1[i]);
        }
        Console.ReadLine();
    }
    */
}